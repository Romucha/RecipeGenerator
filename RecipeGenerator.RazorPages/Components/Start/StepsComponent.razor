<fieldset>
    <legend>Add Steps</legend>
    <div>
        <div style="display: flex; flex-direction: column">
            <div style="display: flex; flex-direction: column">
                <button class="btn btn-primary" @onclick="StartVM.AddStepCommand.Execute">Add step</button>
                @if (StartVM.StepList?.Any() == true)
                {
                    foreach (var step in StartVM.StepList)
                    {
                        <StepComponent SelectedIngredients="@StartVM.IngredientList" Step="@step"/>
                            
                    }
                }
            </div>
        </div>
    </div>
</fieldset>